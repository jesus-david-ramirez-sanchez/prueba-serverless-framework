{
    "name": "library-shop-api",
    "version": "1.0.0",
    "description": "API para gestión de libros con Serverless Framework",
    "main": "index.js",
    "scripts": {
        "deploy": "serverless deploy --offline",
        "deploy:dev": "serverless deploy --stage dev --offline",
        "deploy:prod": "serverless deploy --stage prod --offline",
        "remove": "serverless remove --offline"
    },
    "keywords": ["serverless", "aws", "lambda", "javascript"],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "serverless": "^4.18.1"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.450.0",
        "@aws-sdk/lib-dynamodb": "^3.450.0",
        "uuid": "^9.0.1",
        "joi": "^17.11.0"
    }
}