{
    "name": "library-shop-api",
    "version": "1.0.0",
    "description": "API para gestión de libros con Serverless Framework",
    "main": "index.js",
    "scripts": {
        "dev": "serverless offline",
        "deploy": "serverless deploy",
        "deploy:dev": "serverless deploy --stage dev",
        "deploy:prod": "serverless deploy --stage prod",
        "remove": "serverless remove",
        "logs": "serverless logs -f createBook -t",
        "docker:build": "docker build -t library-shop-api .",
        "docker:run": "docker run -p 9000:9000 library-shop-api",
        "docker:compose": "docker-compose up --build",
        "docker:compose:down": "docker-compose down",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": ["serverless", "aws", "lambda", "javascript"],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "serverless": "^3.38.0",
        "serverless-offline": "^13.3.0"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.450.0",
        "@aws-sdk/lib-dynamodb": "^3.450.0",
        "uuid": "^9.0.1",
        "joi": "^17.11.0"
    }
}